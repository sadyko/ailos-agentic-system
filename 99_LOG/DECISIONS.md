2026-06-30  STEP_01  PASS  150f36d524af6eb14bb2182710ebd8813b8ff33f
2026-06-30  STEP_01  PASS  0496c0e9b0f206c6820ff18b713d56a17144ce0c
2026-06-30  ui  STEP_01  PASS  47a3bee2833928ce2cfd39039e2a8f05c6405d29

## 2026-07-10 — EasyMed small-clinic features: Phases 1–4 deployed live (Phase 0 DDL staged)
- All SPA + gateway code for custom services, daily stationary billing, drug orders and Rx handoff is live on 45.77.242.169 (details: docs/superpowers/plans/2026-07-04-easymed-clinic-features.md → EXECUTION STATUS).
- Every new path is forward-compatible: with the DDL not yet run, the gateway flags endpoint returns all-false defaults and the SPA behaves exactly as before (verified live). Migration staged at /var/www/easymed.uz/supabase/migrations/102_clinic_features.sql; on-server + appointment-mvp Supabase PATs are revoked (401) so the owner runs the SQL (dashboard or apply_102 runner with a fresh token).
- Plan deviation (Phase 3): EasyMed already had atomic SECURITY-DEFINER dispense RPCs (dispense_visit_item / dispense_admission_item / void_dispensed_*) wired to «Выдать препарат» in the workspace and admission modal. Kept them as the billing/stock engine; added the missing spec pieces only — med_administrations journal mirror on stationary dispense (GIVE_DRUG_LOG_V1) and a per-Rx «Выдать» handoff button (RX_DISPENSE_V1, item picker prefiltered by Rx name). The new gateway POST /api/v1/inpatient/administer-drug was still deployed and fully data-tested (log+bill+stock, rollback, negative-stock guard) as the unified API path.
- Cache-busting unified: 13 modules retagged ?v=clinic1 across the import graph (fixed the pre-existing vm14/vm15 double-instance of visit-modal).

## 2026-07-10 — EasyMed custom-services dialog + Excel: fixed the broken pieces (live)
User report: custom-service dialog "looks terribly wrong", can't edit Type/Category, groups render wrong, Excel Sample/Import/Export not working, no "from catalog" path. Root causes + fixes (all live on 45.77.242.169, verified):
- **Excel dead for EVERYONE (not just custom):** site CSP `script-src` omits cdn.sheetjs.com, so the SheetJS dynamic import was blocked → Sample/Import/Export silently failed. Fix (EXCEL_SELF_HOST_V1): self-hosted xlsx-0.20.3 under /js/vendor/ served as **.js** (‘self’ is CSP-allowed; .mjs served as octet-stream which browsers refuse for modules). Verified end-to-end via Node round-trip (build→write→read xlsx).
- **Type/Category dropdown showed cross-clinic duplicates** ("Диагностика" ×4): gateway `list_service_catalog` returned every company's types unfiltered. Fix (LOOKUPS_CATALOG_V3): company-aware — custom clinics get ONLY their own rows + rows referenced by their own services (labels still resolve), deduped; shared clinics unchanged. Cleveland now returns 4 clean types / 6 cats. This one endpoint feeds form dropdowns, picker/appointments grouping, export labels and import resolution.
- **Couldn't create own Type/Category:** searchableSelect was pick-only. Fix (CUSTOM_CLINIC_V4): added optional onCreate → a "＋ Создать «X»" row; types/categories via gateway create-or-get (POST /lookups/catalog, company-scoped, RLS-safe), departments via direct insert. Verified create-or-get is idempotent.
- **Entry flow:** custom clinic "+ Add service" now opens the clean own-service form directly (title «Своя услуга клиники»); added a header **«Из каталога»** button so they can still pull medcore catalog services; catalog wizard keeps its request-a-service path. Shared clinics unchanged (catalog wizard on Add).
- Company flag was originally enabled on the wrong company (cf38df6f VITA); corrected to 2f308e90 Cleveland (the cleveland.easymed.uz login's company). Cache versions → clinic6.

## 2026-07-10 — EasyMed service dialog: grouped 2-column layout (SERVICE_FORM_GROUPS_V1)
The custom-service create/edit dialog rendered as one cramped column with "Internal code" up top. The generic CRUD form already supports a wide (1120px) sectioned 2-column layout (mg-section/mg-grid) — the roles editor uses it — the services section just never opted in. Added a `groups` config to the services section: Основное (name/type/category/department) · Цена, налог и длительность (price/VAT/duration/requires_doctor/doctor%/tube) · Врачи (full-width) · Дополнительно — код и фискализация (code/IKPU/NNM/active). No new code, existing CSS; applies to every clinic + both modes. Cache → clinic7.

## 2026-07-10 — EasyMed custom-service Type/Category/Dept: type-your-own (CREATABLE_COMBO_COMMIT_V1)
User: "category is dropdown from existing is not okay". Root cause: the creatable combobox discarded typed text on blur unless the user explicitly clicked the "＋ Создать" row, so a typed-but-unconfirmed category vanished — feeling like a locked pick-list. Fix: a typed value now auto resolves (pick existing OR gateway create-or-get) on Enter or blur; placeholder → «Выберите или впишите новую…». Applies to custom-clinic service Type/Category/Department; non-creatable comboboxes unchanged. Cache → clinic8.

## 2026-07-10 — EasyMed drug/products catalog editor built (ITEMS_CRUD_V1, live)
Gap found: the drug list (clinic_items) had NO editing screen — Pharmacy's «Товары» button linked to settings:clinic_items, a section that was never built ("Section not found"), so no way to add a drug / set price / mark «Препарат». Every prescription/stationary/dispense flow depends on this list.
Build: clinic_items is a company-local table with no RLS policy in the tracked schema (created out-of-band), so — rather than gamble on browser writes on a live clinical system — writes go through a new allowlisted service-role gateway CRUD (POST/PATCH/DELETE /api/v1/crud/{table}, allowlist={clinic_items}, company_id forced from current_em_user, cross-company scoped). Verified end-to-end: create/update/delete work, injected company_id/id ignored, non-listed tables rejected, cross-company delete = 0 rows.
- Added a generic `gatewayCrud` flag to the section-crud save/delete and the Excel importer, so any RLS-guarded section can reuse this. clinic_items section «Товары и препараты» (group Service settings) with grouped form: Основное (name/is_drug/price/unit) · Детали (form/strength/tax_rate/code) · Статус. Reads stay on the browser client (clinic_items is readable). Pharmacy «Товары» link now resolves.
- Excel Sample/Import/Export for clinic_items (gateway-routed batch insert + update-existing by name). Stock stays in Снабжение (Receive) per existing architecture (item_stock via stock_movements trigger — never written directly).
- Permissions: owner/super-admin = full access; unlisted key defaults to 'editor' for roles, so staff can manage drugs by default. Cache → clinic9.

## 2026-07-10 — EasyMed integration verification: 22/22 green (1 bug caught + fixed)
Ran a full cross-feature integration test on the live system (self-cleaning, finally-guarded). It caught a real bug: the unified administer-drug gateway endpoint wrote med_administrations unconditionally, but med_administrations.admission_id is NOT NULL (stationary nurse journal) → 400 on the outpatient/visit path. Fixed (ADMINISTER_DRUG_V2): journal is written only for admission context; outpatient records via the visit_services line + stock movement (matches dispense_visit_item). Not user-reachable (UI uses the dispense RPCs), but the endpoint is now correct. Re-run: 22/22 pass — flags per clinic, company-scoped catalog (no dup names), taxonomy create-or-get, drug CRUD (company forced + allowlist + cross-company scoped), outpatient administer (bill+stock, no journal), stationary administer (bill+stock+journal linked to admission), negative-stock guard, daily-billing columns live + default hourly, custom services core_service_id=null (Symptex-invisible). Zero test rows left. SPA: all JS syntax clean, all versioned imports resolve, site + self-hosted xlsx serve, gateway healthy, no 500s.

## 2026-07-10 — EasyMed: custom-catalog mode made the platform standard (CUSTOM_SERVICES_STANDARD_V1)
User wanted the per-clinic custom-catalog capability standardized for ALL clinics + new clinics, with no per-clinic switch. Verified first that it's safe: every clinic's services already carry a local type_id (only 1 corelmed service platform-wide lacks one), so forcing custom mode does NOT break any clinic's picker grouping; the medcore catalog stays reachable via the «Из каталога» button, and catalog services keep core_service_id → Symptex unaffected.
Implementation is gateway-only (the SPA reads the flag at runtime, so no SPA redeploy / cache bump): `company_flags` now always returns custom_services_enabled=True, and `list_service_catalog` always uses the company-scoped branch. No DB migration and no per-company data changes — new clinics get custom mode automatically even though the column still defaults false (the code overrides it). The per-company column is retained for possible future per-clinic control. Verified custom=True for Cleveland/akfa/Novomedics/corelmed. Users with an admin tab already open need one reload (flags are cached per session). Deployment is one shared SPA (/var/www/easymed.uz behind *.easymed.uz) + one gateway, so all clinics run identical code — confirmed all subdomains serve admin.js?v=clinic9.

## 2026-07-10 — EasyMed: clinic admin can now ALSO be a doctor (ADMIN_DOCTOR_V1)
Design flaw: "is a doctor" and "permission role" were conflated. actorFromUser hard-coded isDoctor = !isAdmin && (...), so a user was admin XOR doctor, never both. Worse, the employee editor's "This employee is a doctor" toggle auto-assigned the Doctor permission role, and a DB trigger flips users.role text to match — which turned off is_admin and locked the admin out of Settings (this hit corelmed's owner: role became 'doctor', role_id=Doctor). Fix (3 SPA files):
- auth.js actorFromUser: isDoctor = role==='doctor' || is_doctor===true || specialty || license (decoupled from is_admin) — a user can be both.
- permissions.js scopedDoctorId: admins (super admin OR clinic admin) are never patient-scoped, even when also a doctor — they see all data; only pure doctors are scoped.
- employee-editor.js: the is_doctor toggle no longer reassigns the role when the user is an admin (guard on role!=='admin'), so marking an admin a doctor keeps their full access.
Data restore: set corelmed owner's role_id back to the admin role (trigger → role text 'admin'), kept is_doctor=true → now admin+doctor. applyActorPermissions already grants is_admin full access, so an admin-doctor keeps Settings AND appears in doctor lists / has the consultation workspace. Regular doctors unchanged. Loader bumped admdoc1; permissions.js/auth.js are imported version-less so a HARD refresh (Ctrl+Shift+R) is needed to pick them up (plain F5 restores admin access via the data fix, hard refresh adds the doctor capability).

## 2026-07-10 — EasyMed: admin-doctor can edit own profile (ADMIN_DOCTOR_V2)
The ADMIN_DOCTOR_V1 scopedDoctorId change (admins → null so they see all data) broke the consultation «Мой профиль» tab, which passed scopedDoctorId() as the doctor id to renderDoctorProfile — so an admin-doctor got null → "нет контекста врача". Root: scopedDoctorId conflated "am I patient-scoped" with "which doctor am I". Fix: added permissions.js `selfDoctorId()` (user's own id when is_doctor, even if admin) and consultation.js «Мой профиль» now uses it. All other scopedDoctorId sites (appointments/patients/dashboard) are data-scoping and correctly stay null/all for admins. Bumped consultation.js + loader admdoc2; permissions.js is version-less so HARD refresh needed.

## 2026-07-10 — EasyMed admin-doctor: the ACTUAL fix (ADMIN_DOCTOR_V3)
V1/V2 couldn't take effect: the current-user SELECT in auth.js (rehydrateUserFromSession + the session helper) never loaded the is_doctor column, so actorFromUser saw role='admin' with no specialty → is_doctor=false → selfDoctorId()=null → «Мой профиль» blank. Fix: added is_doctor to both user-load selects. Runtime path now: rehydrate (is_doctor) → actorFromUser (is_doctor=true) → state.user.is_doctor=true → selfDoctorId()=own id → profile renders. Bumped auth.js refs + loader admdoc3 (auth.js was imported ?v=tu1, unchanged, so the old cached copy without is_doctor kept loading). HARD refresh required. Lesson: when adding a runtime flag, verify the column is actually SELECTed in the user-load query, not just present in the DB.

## 2026-07-10 — EasyMed: «Раздел» routing group for custom/Excel services (SERVICE_GROUP_ROUTING_V1)
Problem: services added by Excel/form (not from medcore catalog) had no routing signal, so lab tests/procedures all fell into the doctor cabinet. Inspected the live routing: it keys on services.type ('lab'→#labs, 'procedure'→#procedures) + departments.kind; services.type is the reliable column (checked first, survives join failures). Fix:
- New «Раздел» classifier → services.type. Form: a constrained native <select> (Консультация/Лаборатория/Процедуры/Диагностика/Хирургия). Excel: a `group` column with strict validation (invalid value = import error) AND a REAL native Excel dropdown in the Sample download.
- Native Excel data validation: SheetJS community can't write it, so self-hosted fflate (/js/vendor/fflate.js, served .js) and post-process the xlsx zip — inject <dataValidations> after </sheetData>. POC + replicated-build verified (dropdown on col B, 5 values, file still valid).
- Routing made services.type-driven: procedures.js now also routes services.type==='procedure'; consultation.js excludes it from the cabinet. ADDITIVE (no existing service uses type='procedure', verified) so no live routing changes for current data.
- GOTCHA caught by verification: services.type has a CHECK constraint = {consultation,lab,imaging,procedure,other} — 'surgery' is REJECTED. Mapped Хирургия→'other' (cabinet), Диагностика→'imaging' (cabinet). All 5 group values now valid.
- Symptex unchanged: custom services (core_service_id=null) route internally by «Раздел» but stay off symptex.uz; marketplace clinics use the medcore catalog. Cache → route2.

## 2026-07-10 — Symptex: doctor-detail consultation price cards fixed (DOC_SVC_CARDS_V1)
Doctor detail page («Консультации и цены») cards looked broken on desktop + mobile: inconsistent alignment (short titles centered, long ones left), "0 сум" shown for a "coming soon" item. Root cause: the cards reused the overloaded `.svc-item` class which is defined TWICE with conflicting intent — inline in doctor_detail.html (list row, top-border) vs symptex.css:762 (bordered card built for different markup w/ svc-item__n/__go). Under the mobile media query the flex row collapsed to a centered column, so a block-level title block appeared centered when short and left when long. Fix (Symptex = /var/www/symptex-next, symptex-next.service Gunicorn): gave the consultation cards a DEDICATED class in doc-profile.js (servicesSection render → .doc-svc-grid/.doc-svc/.doc-svc__t/__d/__p/--soon) + clean self-contained CSS in doctor_detail.html inline <style>: responsive grid (auto-fill minmax(228px) → 2-col desktop, 1-col mobile), consistent left-aligned title + price-at-bottom, tidy soon state (grayed + "Скоро" instead of "0 сум"). Bumped doc-profile.js?v=p9→p10; restarted symptex-next for the template change. Verified: doctor page 200, loads p10, inline CSS has .doc-svc-grid. NOTE: "методомперемещения" (missing space) is a DATA typo in the consultation title, not a layout bug.

## 2026-07-10 — EasyMed: fix Excel services import RLS rejection + simplify columns (SERVICES_GATEWAY_V1)
"Import failed — 370 rows rejected: new row violates RLS for services." Root cause: the services INSERT RLS policy (migration 066) = `current_user_is_admin() AND company_id=current_user_company_id() AND core_service_id IS NOT NULL` — a clinic admin can only insert CATALOG services; custom services (core_service_id NULL, the whole Excel/custom feature) are rejected by the DB. That's also why the catalog wizard worked but custom didn't. Fix: added `services` to the gateway CRUD allowlist and set the services section + import config `gatewayCrud: true`, so custom-service writes go through the service-role gateway (bypasses RLS, company_id forced) — same pattern as clinic_items. Verified: mixed 4-row batch inserts 4/4, 0 RLS rejections, all core_service_id NULL + own company. GOTCHA fixed: PostgREST batch insert requires UNIFORM keys — crud_insert now unions keys + fills missing with None (CRUD_UNIFORM_V1) so a mixed-key batch never 400s.
Also simplified the importer to the 7 columns the user asked for: name, group, price, tax_rate, duration_minutes, requires_doctor, active (name/group/price required; the rest default 12/30/true/true). Dropped type/category/department/ikpu/nnm/tube_color/code. The `group` now does double duty: sets services.type (routing) AND, via a transform, mirrors into a service_type (type_id) for picker grouping — so the 7-col sheet still routes AND groups. Native Excel dropdown on `group` retained (col B). Cache route3.

## 2026-07-10 — EasyMed gateway CRUD route never parsed its body (CRUD_BODY_FIX)
Import still failed with "[object Object]" after SERVICES_GATEWAY_V1. Root cause: `POST /api/v1/crud/{table}` was defined `crud_insert_route(table, body, em=...)` with **`body` unannotated** → FastAPI treated it as a query param → every request 422'd (the "[object Object]" was FastAPI's validation-error array shown by gw()). This silently broke BOTH the services import AND the drug-catalog add (same route). Fix attempt `body: list` CRASHED the gateway on boot (FastAPI treats a bare `list` body as form-data → needs python-multipart, not installed → all clinics' gateway features down briefly). Correct fix: `from fastapi import Body` + `body: list = Body(...)` (forces JSON body). Gateway back up, route now parses the body (403 auth not 422), end-to-end services batch inserts 2/2 with 0 RLS rejections. Lesson: a JSON-array FastAPI body param MUST be `= Body(...)` (a bare `list` annotation → form-data → multipart crash; unannotated → query param → 422). crud_update's `body: dict` was fine (dict bare = JSON body).

## 2026-07-10 — EasyMed: restore type/category/department import cols + company-scope the catalog (SERVICE_IMPORT_TYPECAT_V1)
After the 370-row import, group→services.type worked and type_id was set, but: (a) category/department were NOT imported — I had wrongly REMOVED those columns when the user asked to make them "not required" (they meant optional); (b) type_id was CROSS-COMPANY (4/5 pointed at VITA/Cleveland's service_types) causing duplicate "Диагностика" in the clinic's catalog — because create_catalog_lookup matched service_type/category names across ALL companies. Fixes: restored type/category/department as OPTIONAL importer columns (fk autoCreate; blank type → type_id still derived from group via the transform); create_catalog_lookup now matches WITHIN the caller's company (company_id filter) so each clinic owns its own types/categories (LOOKUPS_CATALOG_CREATE_V2). Verified: two clinics + same type name → different ids; import insert sets type_id + category_id both owned by the clinic. To fix the 370 already-imported services: re-import with "Update existing rows when name matches" checked (fills category + re-maps type to a company-scoped one, no duplicates). Cache route4.

## 2026-07-10 — EasyMed: service-workspace responsive + collapsible side panels (WS_RESPONSIVE_V1)
Doctor workspace (#service-workspace) 3-col grid (clamp(260) 1fr clamp(290), sticky sidebars) overflowed on small monitors — the sidebar minimums starved the center so its content overlapped the panels. Fix (service-workspace.js): moved the grid to a CSS class `.ws3` (injected <style>) with collapse modifiers (.ws3--nl/.ws3--nr) + `.ws-hide` on the panel; added two toggle chips («Пациент» left, «Заметки · ИИ» right) that hide/show each side panel so the center reflows to full width (no overlap). Responsive: <1180px auto-collapses both sidebars by default (center gets room), <900px forces single-column stack + un-sticks panels; state persisted in localStorage (em-ws-layout). Cache → wsr1. Design per sn-ui-checklist/web-design-guidelines: clear pill affordances w/ hover+off states, consistent spacing, grid reflow (never overlap), remembered preference.

## 2026-07-10 — EasyMed: documents showed fake INN/license when unset (DOC_REQUISITES_V1)
The document header/footer (doc-settings.js) had hardcoded demo defaults taxId='INN 305 412 887' / license='License №UZ-MED-04188 · valid until 2028', only overridden if the company set tax_id/license_number. So a clinic without them (e.g. corelmed) printed the fake requisites on real documents. Fix: emptied the two defaults + render the requisites line only when actually present — in the footer (`(taxId||license) ? ... filter(Boolean).join(' · ')`) and the fiscal receipt (drop the INN line when unset). Real values still show once the clinic fills companies.tax_id/license_number. Cache docreq1. (Same demo-default pattern exists for address/phone/email/web but those are usually set; left as-is.)

## 2026-07-13 — EasyMed: admin-doctor missing from every doctor list (ADMIN_DOCTOR_LIST_V1)
A clinic admin who is also a doctor (users.role='admin', is_doctor=true, no specialty/license — e.g. corelmed owner "музаффар аллаберганов") did not appear in any doctor picker: examination/workspace, «Календарь записи», appointments, consultation dashboard, beds, referrals. Root cause: 6 doctor-detection filters keyed on `role==='doctor' || specialty || license` but NOT `is_doctor`, so an admin-doctor was dropped. Fix: added `is_doctor === true` to all 6 filters (data.js loadDoctors, consultation.js, beds.js, room-calendar.js, service-picker-modal.js, visit-modal.js) and added `is_doctor` to the 4 SELECTs that omitted it (consultation/beds/room-calendar/visit-modal). requests-inbox already used `.eq('is_doctor', true)` — untouched. Transactional patch (stage-then-write, .bak-adlist backups), node --check all green. Cache: 5 tagged modules → ?v=adlist1 across every importer (8+3+2+1+1), admin.html entry admin.js?v=adlist1; data.js is tagless (busts on hard-refresh, revalidates ≤4h). Durable invariant: doctor detection anywhere in the SPA MUST include is_doctor — admin-doctors have no role='doctor'/specialty signal.

## 2026-07-14 — Symptex: OTP login "incorrect code" + "logged-in can't book" + gateway-timeout (SX_AUTH_BOOKING_FIX)
Root-caused the full register→OTP→login→book path on live symptex.uz (/var/www/symptex-next, gunicorn symptex-next) with live DB + gateway evidence, then adversarially verified each fix in a 9-agent workflow (wf_14182355-40c) before touching prod. Shipped in 3 backed-up, py_compile/node-checked, auto-restoring batches (backups *.bak-sxfix/2/3):
- **BUG2 "incorrect code" (the reported bug):** every "resend" issues a NEW otp_codes row without invalidating the old ones (live DB: one phone had 3–6 co-pending DISTINCT codes), but `otp.verify()` checked ONLY the single newest (phone,'login') row, while delivery ships a different row's code → valid delivered code rejected. Fix (otp.py OTP_VERIFY_ANY_V1): accept the typed code if it matches ANY currently-valid (unconsumed/unexpired) row for the exact (phone,purpose) — **kept `.eq('purpose','login')` as a HARD invariant** (a partner 'register' code must never auth a patient); brute force bounded by MAX_ATTEMPTS on the newest row (recoverable via resend) + route limits; consume ALL pending rows on success. Also scoped `deliver_by_contact` to `purpose='login'` so a newer partner 'register' code can't be shipped through the patient contact-share.
- **NEW1 (critical ACCOUNT-TAKEOVER the workflow caught):** bot_webhook accepted a *forwarded* contact card with NO `user_id`; an attacker could trigger a victim's OTP then share the victim's number to receive the code. Fix (CONTACT_OWN_ONLY_V1): require `contact.user_id == sender`.
- **BUG3A "logged in but can't book":** the Telegram Login Widget creates a phone-less user; `booking.create()` needs a phone → 400 no_phone → booking.js bounced the (already logged-in) user back to /login whose primary CTA is the same widget → loop. Fix: booking.js sends no_phone users to `/login?need=phone` (widget hidden, phone-OTP shown); `next` is threaded through /auth/otp/verify so they return to /booking; `sb_get_or_create_user` now MERGES the verified phone onto the existing telegram_id row (NO_USER_FORK_V1) instead of forking a 2nd account — so booking then passes.
- **BUG1 UX ("code only after /start"):** OTP_ENUM_FIX removed the `sent` field but login.html still branched on it, so everyone (incl. users the code was already pushed to) was told to open the bot + press Start. Fix: one honest, non-asserting dual-path message (no enumeration oracle).
- **BUG3B intermittent "no free time":** `/availability` used a 6s read timeout vs a gateway that answers in 3–6s+ (cold ~29s); timeouts returned an empty payload rendered identically to "no slots". Fix (AVAIL_TIMEOUT_V1): 12s read timeout; on real failure return `error:'unavailable'` and booking.js shows a **retry** state (not the "pick another doctor" dead-end); create_visit read timeout 8s→12s; rate-limited the public availability route.
**Deferred infra follow-ups (recommended, need owner sign-off):** flask-limiter has no shared storage → per-worker limits (extensions.py); create_visit lacks an idempotency key (re-click can double-book a real visit); no short-TTL availability cache / gateway keep-warm for the ~29s cold case. Verified live: login/booking 200; availability ok=true 6 days in 1.9s; need=phone hides widget; no errors post-restart.

## 2026-07-14 — Symptex: keep-warm + double-submit hardening (owner-approved follow-ups, Batch D)
Follow-ups to SX_AUTH_BOOKING_FIX, deployed live (backups *.bak-sxfix4):
- **KEEP_WARM_V1 (core_gateway.py):** the 12s availability read timeout doesn't cover a genuine ~29s COLD gateway. Added a periodic keep-warm daemon (every 4 min, per worker): re-warms doctors/clinics cache + touches /availability for up to 6 published doctors, so the first booking after a quiet period never cold-starts. Verified live: `_keep_warm_once()` runs clean in ~18s, 8 doctors published. Closes the cold-start gap the review flagged.
- **AMBIGUOUS_SUBMIT_V1 (booking.js → p13):** finding — the doctor+slot VISIT double-book is ALREADY prevented by the EasyMed gateway (`POST /api/v1/visits` re-checks offered slots → 409 + a DB EXCLUDE constraint, SLOT_CONFLICT_V1), so the clinical create path was left untouched. Hardened the client instead: on an ambiguous network/gateway error the user is steered to «Мои записи» to verify before resending (not a blind retry that could duplicate a lead), retry only on explicit choice.
- **Still deferred (needs infra sign-off):** flask-limiter shared storage (Redis) — current per-worker in-memory limits are weaker than ideal for DoS/brute-force bounding. Not urgent at current traffic (1 clinic / 8 doctors).

## 2026-07-14 — EasyMed: Товары «Приход-расход» ledger + import/permissions review (ITEMS_LEDGER_V1)
On the Товары и препараты page (#settings:clinic_items), owner wanted: upload drugs w/ prices, приход-расход calculations, and view/edit/delete permissions. Findings + build:
- **Upload drugs w/ prices — already worked** (ITEMS_IMPORT_V1): Sample/Import Excel on the page; columns name(req)/is_drug/price/unit/form/strength/tax_rate/code/active; matchField=name (re-import updates); writes via service-role gateway (clinic_items RLS blocks browser inserts).
- **Permissions — already worked**: `settings:clinic_items` («Товары и препараты») is auto-listed in the role editor (permissionGroups builds one per non-hidden section w/ a table). Grant View (read-only), Edit (editor), or Delete (admin level). section-crud already gates Edit/Delete on canEdit/canDelete(currentPermKey='settings:clinic_items'); accessLevelFor defaults a granted-but-unlevelled key to 'editor', delete needs explicit admin.
- **NEW приход-расход (the build):** new module `js/admin/views/items-ledger.js` (renderItemsLedger) + a «Приход-расход» toolbar button on clinic_items (section-crud.js). Per the owner's answers: CONSOLIDATED for all items (not per-drug), valued AT COST. Shows Приход/Расход/Остаток summary + a movement list (all items, chronological) with period (этот/прошлый месяц·30д·всё) + branch filters. «+ Добавить приход» form (Товар · цена за ед. · ед. · количество) records a stock_movements receipt; a brand-new drug is created via gateway CRUD then received. Cost basis for расход/остаток = each item's LATEST receipt unit_cost; приход uses actual receipt cost. Reads stock_movements/item_stock/clinic_items directly (browser-readable; RLS admin writes for stock_movements). Respects settings:clinic_items view (open) + edit (add). Cache: new module + section-crud → ?v=ledger1, admin.js entry → ledger1. Live-verified: all 9 columns used exist on the tables; import chain serves 200. Tables currently EMPTY (no drugs/stock yet — this page is how the clinic populates them).

## 2026-07-14 — EasyMed: партия (batch приход) Excel import + drug-import/platform-wide verify (ITEMS_LEDGER_V2)
Owner uploads a pharmacy stock report ("Отчёт по остаткам…": Наименование·Количество·Единица·Общая стоимость·Общая стоимость без наценки·Партия) and wants приход-расход to include it.
- **New «Партия из Excel» importer** on the Приход-расход page (items-ledger.js): parses the sheet → per row upserts the drug (creates via gateway CRUD if new, batched) + inserts a `stock_movements` receipt (qty, unit_cost = total_cost÷quantity) → flows straight into приход/остаток. Sample «Образец» (name·quantity·unit·total_cost·total_price). ROBUST: `sheetToObjects` auto-detects the header row (skips the report's title/blank rows) and `parseBatchRow` accepts BOTH English sample keys AND Russian headers — so the RAW pharmacy file uploads with no editing. Validated in Node against the real 129-row file: header found at row 3, **124/125 rows import**, per-unit cost correct (трубка 384000÷30=12800; Изофлуран 1260000÷2=630000). Chunked movement insert (200/batch). Edit-gated (canEdit settings:clinic_items). Cache chain → ledger2.
- **Drug catalog import checked (no services-style drop):** buildRow maps direct columns — price/tax_rate via numeric coerce, is_drug/active via bool, name/unit/form/strength/code as text, with case-insensitive header normalisation. The old services bug was FK-specific (type/category lookups); clinic_items has NO FK columns, so that class of error cannot occur.
- **Platform-wide CONFIRMED:** live nginx `/etc/nginx/sites-available/easymed.uz` — `server_name easymed.uz www.easymed.uz ~^(?<slug>[^.]+)\.easymed\.uz$` + `root /var/www/easymed.uz`. ONE wildcard vhost, ONE docroot for every `<slug>.easymed.uz` (cleveland/corelmed/vita/…) + any NEW clinic. All EasyMed code edits (this ledger + importer + every prior fix) are inherently all-clinics; only data/flags are per-clinic. Same for the gateway (single /opt/easymed-api).

## 2026-07-14 — EasyMed: doctor workspace redesign — 2-col + flexible document (WS_CONSOLIDATE_V1 / WS_FLEX_DOC_V1)
service-workspace.js (#service-workspace), owner-approved redesign, deployed live (backup service-workspace.js.bak-wsflex; cache service-workspace.js?v=wsr2, admin.html admin.js?v=wsr2):
- **WS_CONSOLIDATE_V1 (layout):** collapsed the 3-column grid to TWO columns (document + one side panel). Removed the redundant left patient card (patient info is already in the top liveStrip); moved every left-panel clinical card (Диагноз, Услуги приёма, Рекомендации, Рецепт, Назначения в клинике, Хронические состояния) into the right panel; relabelled the top «Open full chart» → «Карта пациента»; dropped the «Пациент» left toggle (kept a «Боковая панель» right toggle). Grid CSS + wsToggleChip updated.
- **WS_FLEX_DOC_V1 (document):** per the owner — the приём document now opens in a SIMPLE section form by default (the printed A4 template stays available via a new «Бланк» toggle in the doc bar). Sections (Жалобы·Анамнез·Лаб·Инстр·Осмотр·Диагноз·Терапия·Рекомендации) are add/remove-able via a +/- section bar (chips with × + «+ Добавить раздел» dropdown); default set = Жалобы·Осмотр·Диагноз·Рекомендации. NOTHING is mandatory (REQUIRED_SECS=[]), so save/«Завершить приём» aren't blocked by empty fields. Removed sections get class a4-sec-off (display:none) and are EXCLUDED from save+print (collectFields skips [data-field] inside .a4-sec-off; buildBlankData reads collectFields so the printed Бланк matches). On re-open, applyFields reveals any section that has saved content. wsState.docSections reset per consultation. Diagnostics services still open in the imaging Бланк.
- **CONCURRENCY GOTCHA:** the shared docroot had a CONCURRENT session mid-task — it bumped service-picker-modal.js aurora67→tpl1 (in service-workspace.js + admin.html admin.js?v=tpl1) between my pull and deploy. My deploy (based on the older pull) reverted that ONE import line. Caught via md5(pulled base) ≠ md5(pre-deploy backup) → diff showed the single-line delta → restored service-picker-modal.js?v=tpl1 (all 8 importers consistent) without clobbering their work. LESSON on the shared /var/www/easymed.uz: before overwriting a whole file, md5 the pulled base vs the live file; reconcile via diff, don't blind-overwrite.

## 2026-07-14 — EasyMed: workspace doc follow-ups — no-blank, doctor phone, paste results (WS_PASTE_V1 / WS_DOCPHONE_V1)
service-workspace.js follow-ups, applied IN-PLACE (anchored patch, concurrency-safe) + node-check + backup service-workspace.js.bak-wsb3; cache service-workspace.js?v=wsr3, admin.html admin.js?v=wsr3:
- **Removed «Бланк» toggle** (owner: "we don't need a blank"): the doc editor is the section form; the printed clinic document is produced by «Печать» (handlePrint builds it from the fields + clinic name/logo). The blankBtn was replaced by the phone toggle; setBlankMode default stays form (diagnostics services still open in their imaging template).
- **WS_DOCPHONE_V1 — optional doctor phone:** a «Тел. врача» toggle in the doc toolbar adds an editable phone line to the ВРАЧ footer (prefilled from currentUser().phone, editable). Off by default; like the sections it's class a4-sec-off until added, so collectFields excludes it from save/print until on; applyFields reveals it if a saved phone exists; buildBlankData exposes data.doctorPhone for templates that have the slot.
- **WS_PASTE_V1 — «Вставить результаты» now actually pastes:** the results popup's viewer (openEmrResultModal) gained a «Вставить в документ» button. fillLabResults/fillServiceConclusion now RETURN the pasteable html; the button reveals the target section then a4InsertHtml's it in — labs→ЛАБОРАТОРНЫЕ(labs_text), diagnostics→ИНСТРУМЕНТАЛЬНЫЕ(instrumental_text), service conclusion→ОСМОТР(physical_exam). Previously the popup only VIEWED (footer=Закрыть only) → "not pasting".
- **Renamed** «Карта приёмов» → «Вставить результаты» (button + popup header).
- **ANSWER (categorization):** functional/laboratory/diagnosis is decided by each service's DEPARTMENT KIND — `services.departments.kind` ∈ laboratory|diagnostics|procedure, configured in the clinic's Departments settings (DEPT_KIND_ROUTING_V1) — with a service-type-name regex fallback (isLab/isDiagnostic) for services with no department. Same router as the work-queue.
- **CONCURRENCY:** another session bumped admin.html admin.js tag tpl1→tpl3 + service-picker-modal→tpl3 mid-task. In-place anchored patch preserved all of it (no whole-file overwrite); my admin.html bump used a regex on any current tag.

## 2026-07-14 — EasyMed: workspace patient-card + clinic-template document (WS_PATIENTCARD_PANEL_V1 / WS_CLINIC_DOC_V1)
service-workspace.js, applied in-place (anchored, concurrency-safe) + backup .bak-wsb4; cache wsr4:
- **WS_PATIENTCARD_PANEL_V1:** removed the vitals strip from the top patient bar; put the patient info card (patientCard — name/phone/DOB/blood/allergies + «Карта пациента», NO vitals) into the RIGHT panel immediately after the main actions (Пауза / Завершить приём), at normal card width. («Витальные» button still records vitals; loadVitals no-ops without the strip.)
- **WS_CLINIC_DOC_V1:** the document now opens in the CLINIC's configured template (#documents → renderBlank/renderDesignedVariant) by DEFAULT (reverting the WS_FLEX_DOC form-default), and setBlankMode keeps the insert toolbar (`[data-toolbar-slot]`) VISIBLE in template mode so «Рекомендации/Рецепт/Диагноз/Функц./Лучевая/Лаборатория» (a4InsertHtml has BLANK_PASTE_V1) + «Вставить результаты» still work. The «Бланк» toggle stays removed.
- KNOWN NUANCES (in template mode the a4-scroll is hidden): the РАЗДЕЛЫ +/- section bar + the doctor-phone footer field are FORM-mode features → not shown in template mode (sections come from the template design; doctorPhone flows to data.doctorPhone but needs a template slot to print). Formatting buttons (execCommand) only affect the form, not the template iframe; insert buttons work in both.

## 2026-07-14 — EasyMed: workspace tidy — remove top patient card + chronic-diseases card, +/- bar visible in template (wsr5)
service-workspace.js in-place (anchored) + backup .bak-wsb5; cache wsr5:
- Removed the TOP patient-info row from liveStrip (the patient card now lives in the right panel after the actions); kept the nav row (Мои услуги back · Live service · service name/doctor).
- Removed the «Хронические состояния» card from the right panel.
- WS_CLINIC_DOC_V1: moved the +/- «РАЗДЕЛЫ» section bar OUT of a4-scroll so it stays visible at the top of the document in template mode (previously form-mode only). It toggles which sections' content flows into the doc (collectFields skips a4-sec-off → buildBlankData → template re-renders via syncSections). Note: with the fixed #documents template the +/- controls CONTENT inclusion; visual section removal depends on the template omitting empty sections.

## 2026-07-14 — EasyMed: workspace — remove top nav strip + inline add/remove sections (WS_INLINE_SECTIONS_V1, wsr6)
service-workspace.js in-place (anchored) + backup .bak-wsb6; cache wsr6:
- Removed the top nav strip (liveStrip) entirely (owner: "remove this shit at the top"). NOTE: this also removed the «Мои услуги» back button — navigation back is now via app/browser; can re-add a compact back control if needed.
- WS_INLINE_SECTIONS_V1: moved «Разделы» add/remove INTO the document list itself. Each section renders as either an editable block with a × (remove) or — when off — an inline "+ Добавить: <name>" placeholder at its position, so the doctor sees which blocks can be added in the list. CSS-swap (no rebuild): off sections keep class a4-sec-off → their .a4-sec-tag/.a4-sec-x/.a4-input hide, .a4-sec-add shows; content stays in the (hidden) input so it survives re-add; collectFields still excludes .a4-sec-off from save/print. Dropped the separate top «РАЗДЕЛЫ» bar (wsSectionManager call removed; functions left dead). Default reverted to the editable document (setBlankMode false) since inline section editing needs the form; «Печать» still renders the clinic #documents template.

## 2026-07-14 — EasyMed: section pills INSIDE the clinic #documents template (WS_TEMPLATE_SECTIONS_V1, wsr7/dv10)
The workspace now uses the clinic's #documents template AS the editor, WITH the +/- section pills the owner liked — resolving the template-vs-flexible-sections tension. In-place patches (anchored) + backups .bak-tplsec:
- **doc-variants.js (dv10):** in EDITOR mode both conclusion variants (classic + compact) now render a section the workspace marked INACTIVE (via d.activeFields) as a "+ Добавить: <name>" pill, and ACTIVE ones with a × remove — for the 8 conclusion sections incl. Диагноз. Print mode (no __editor) and the #documents preview (no activeFields) are unchanged (backward-compatible).
- **service-workspace.js (wsr7):** default back to the clinic template (setBlankMode true); buildBlankData passes activeFields = fields of active sections; _wireBlankEditing injects the pill/× CSS into the iframe and wires data-add→wsAddSection / data-rm→wsRemoveSection (field→section via DOC_SECTIONS). Toggling re-renders the template (syncSections→renderBlank) with updated activeFields; the hidden a4 form stays the data source (collectFields excludes a4-sec-off, so removed sections drop from save/print but their text is preserved for re-add). Insert toolbar + «Вставить результаты» still work (a4InsertHtml BLANK_PASTE_V1). «Печать» = the same template.
- Cache: doc-variants.js?v=dv10 (in service-workspace import), service-workspace.js?v=wsr7 (admin.js), admin.js?v=wsr7 (admin.html).

## 2026-07-14 — EasyMed: doc header cleanup + in-block formatting (WS_BLOCK_FMT_V1, wsr8/dv11/docreq2)
service-workspace.js + doc-variants.js + doc-settings.js, in-place (anchored) + backups .bak-b8:
- **#3 default tagline removed:** DEFAULT_DOC_SETTINGS.tagline 'Care, clarity, precision' → '' (doc-settings.js), so the under-logo tagline only shows when the clinic sets one. Cache doc-settings.js?v=docreq2 (bumped in BOTH importers: service-workspace.js + admin.js — must match).
- **#4 ИНН + лицензия removed from the conclusion header** (doc-variants.js, classic + compact) — dropped the taxId/license `.cl` line from the header block.
- **#1 formatting moved INTO the block (WS_BLOCK_FMT_V1):** fmtToolbar now returns ONLY the insert buttons (Рекомендации/Рецепт/Диагноз/Функц/Лучевая/Лаборатория); the Стиль/размер/Ж-К-Ч-З/цвет/выравнивание/списки controls were removed from the top bar. Instead _wireBlankEditing injects a floating format bar into the template iframe that appears on text selection inside a [data-field] block and runs the iframe's own execCommand (bold/italic/underline/strike/color/highlight/size±/lists/clear) — so formatting finally works on the actual template content, per-block.
- **STILL TODO (owner asked): #2 reorder blocks** — needs the template body to render sections in a dynamic order (wsState.sectionOrder) + ↑↓/drag controls on the pills. Deferred to next step (bigger doc-variants refactor).
